    
    <div class="w-full">
      <div class="px-2">
        <a href="{{ item.url | url }}" class="text-lg font-bold">
          {{ item.data.title }}
        </a>
        <p class="text-sm text-gray-600">{{ item.date | readableDate }}</p>
        <p class="text-gray-700">
          {{ item.templateContent | section | safe }}
        </p>
      </div>
    </div>
    <hr />
    
    {# <li class="mt-6">
      <time value="{{ item.date }}" class="text-gray-600">{{ item.date | readableDate }}</time>
      <h4 class="text-lg font-bold"><a href="{{ item.url | url }}">{{ item.data.title }}</a></h4>
      <div class="">
      {{ item.templateContent | section | safe }}
      </div>
    </li> #}
